# The Court Took My $6.50

This is the public front door.

The court accepted the payment, accepted the paperwork, and then the record still allegedly treated the person as invisible. That contradiction is the pillar.

## Main doors

- `index.html` is the root $6.50 landing page.
- `six_fifty_record/` is the public-safe record pack.
- `free_legal_packet_builder.html` is the browser-only free packet builder.
- `record.html` is the Put Them On The Record lane.
- `case_studies.html` is the public-safe case-study map.
- `projects.html` is the Scientist Poser / projects doorway.
- `texttone.html`, `xray_mirror_dimension.html`, and `companion.html` are project pages.

## Current builder logic

The Free Legal Packet Builder runs in the browser.

It does not submit data.
It does not save data.
It does not require an API.
It generates Markdown the visitor can copy or download.

## Later API path

If submissions or AI generation are added later, the site can move to a host with form handling or serverless functions. The static public front door remains intact.

## Rule

The Court Took My $6.50 is the root.
Projects do not erase the story.
The public record is controlled, readable, and redacted.
The private engine stays offline.

No restart required.


## Speaking-name project map

- `ayane.html` is the presence-engine project.
- `mei_room.html` is the ambient room / interface mood project.
- `xray.html` is the file-visibility project.
- `mirror_dimension.html` is the interface-world project.
- `essays_white_papers.html` is the Scientist Poser essay and white-paper shelf.
- `companion.html` and `xray_mirror_dimension.html` are bridge pages so old links do not break.


## Teaser doors

- `project_teasers.html` is the public-safe screenshot wall.
- `suze_styler.html` is the Suze Styler / Den Deck teaser.
- `mirror_drive.html` is the Mirror Drive teaser.
- `xray.html` maps folders.
- `mirror_dimension.html` gives the tools a world.


## Additional project doors

- `expression_dynamics.html` is the boutique expressive-face / game-character teaser.
- `local_lab.html` is redacted proof-of-work telemetry only.
- `mirror_drive.html` is the Mirror Drive / KITT-but-useful teaser.
